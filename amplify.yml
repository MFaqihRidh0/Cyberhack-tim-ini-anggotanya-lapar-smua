version: 1
frontend:
  phases:
    preBuild:
      commands:
        - bash -c 'cd frontend && npm install'
    build:
      commands:
        - bash -c 'cd frontend && npx prisma generate && npm run build'
  artifacts:
    baseDirectory: frontend/.next
    files:
      - '**/*'
  cache:
    paths:
      - frontend/node_modules/**
